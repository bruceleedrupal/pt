{% extends 'base.html.twig' %}

{% block title %}ReceiveAddress{% endblock %}

{% block body %}
    <h1>ReceiveAddress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ receive_address.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ receive_address.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('receive_address_index') }}">back to list</a>

    <a href="{{ path('receive_address_edit', {'id': receive_address.id}) }}">edit</a>

    {{ include('receive_address/_delete_form.html.twig') }}
{% endblock %}
